import { CsvFile } from "../utils/csvFile";
import { resetDir } from "./fsStatic.test";

describe('fs', () => {
  beforeAll(async () => {
    await resetDir();
  });

  test('function: writeStream', async () => {
    
  });
});
