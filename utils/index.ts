import { CsvFile as C } from './csvFile';
import { FileStaticMethods as F } from './fsStatic';

export const CsvFile = C;
export const FileStaticMethods = F;

export default {
  CsvFile,
  FileStaticMethods,
};
